apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
configMapGenerator:
  - name: app-config
    literals:
      - LOG_LEVEL=info
      - FEATURE_FLAG=true
secretGenerator:
  - name: app-secret
    literals:
      - DB_USER=appuser
      - DB_PASSWORD=s3cr3t
